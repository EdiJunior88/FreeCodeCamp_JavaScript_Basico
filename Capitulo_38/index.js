const myNoun = "dog";
const myAdjective = "big";
const myVerb = "ran";
const myAdverb = "quickly";

// Altere apenas o código abaixo desta linha
const wordBlanks = "On a sunny day I was " + myAdverb + " with " + myAdjective + " velocity " + myVerb + " great " + myNoun + " behind me."; // Altere esta linha
// Altere apenas o código acima desta linha